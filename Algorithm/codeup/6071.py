# 0이 아니면 입력된 정수를 출력하고, 0이 입력되면 출력을 중단해보자.

n = 1
while n != 0:
  n = int(input())
  if n != 0:
    print(n)

# 처음 조건 검사를 통과하기 위해 0 아닌 값을 임의로 저장한다. 

# n이 0이 아니면 n을 출력한다. 

# n이 0이 아닐동안 n은 input 값을 할당한다. 