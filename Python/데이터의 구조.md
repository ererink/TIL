# 데이터의 구조 (Data Structure)

## 메서드 (Method)



 ```python
 a = [10, 1, 100] # 매서드 사용
 new_a = a.sort()
 print(a, new_a)
 # 출력: [10, 1, 100] None
 # 리트스 메서드를 활용하면, 그 메서드를 정렬된 상태로 변경한다. (원본을 변경한다.)
 # return되는 것은 None이다.
 
 b = [10, 1, 100] # 함수 사용 
 new_b = sorted(b)
 print(b, new_b)
 # 출력: [10, 1, 100] [1, 10, 100]
 # sorted 함수를 활용하면, (원본을 변경하지 않는다.) 
 # return되는 것은 정렬된 리스트이다. 
 ```



## 문자열

#### .find()

```python
'문자열'.find('문') #출력:0 #인덱스이다.
'문자열'.find('김') #출력:-1
```

위치를 반환한다. 

없으면 -1을 반환한다. 

#### . index()

```python

```

없으면 오류가 발생한다. 



#### 문자열 관련 검증 메서드

```python
```

순서 상관 없으며 검증(확인)하는 메서드이다. 

#### 

### 문자열 변경

#### .replace

```python
'coone'.replace('o', 'a') #모든 o를 a로 바꿔주세여

'wooooowoo'.replace()
```



#### .strip

```python
'     와우!'
```

양쪽을 제거하거나, 왼쪽을 제거하거나 오른쪽을 제거한다.

보통 공백을 제거한다. 



#### .split

문자열을 특정한 단위로 나눠 (문자를 쪼개서) **리스트로 반환**한다. 



#### 'separator'.join

문자열을 합쳐준다. 

```python
''.join(['3', '5'])
# 출력: 35

names = ','.join(['홍길동', '김철수'])
# 출력: 홍길동,김철수

numbers = ' '.join([10, 20, 100])
print(numbers)
# 오류가 뜬다. 문자열이 입력되어야 하는데 숫자가 입력됐으므로. 

numbers =' '.join(map(str, [10, 20, 100]))
# 출력: 10 20 100 
```

문자열이 아닌 값이 있으면 TypeError가 발생한다. 



문자열은  Immutable이라서 스스로 바뀌는 경우가 없다.

(Immutable은 문자열, tuple , range)



#### 문자열 변경

* replace

* split

* join



## 리스트

* 변경 가능하다. (mutable)



* append()
* pop()
* sort()
* count()



#### .append()

리스트 마지막에 값을 추가한다.



#### .extend(iterable)

반복가능한 것들을 넣으면 추가시켜준다. 

```python
a = ['apple']
a.extend('banana', 'mango')
print(a)
# TypeError가 발생한다. 
# 하나만 들어가기 때문이다. 
#오류를 수정하기 위해서 리스트로 묶어줘야 한다. 

a.extend('banana')
print(a)
# 문자열을 넣어줬네? 하고 글자 하나씩 출력된다. 
# 'b', 'a', 'n'
```



#### .insert(i, x)

정해진 위치에 값을 끼어 넣을 수 있다. 



#### .remove(x)

값이 x인 것을 삭제한다.

x가 없는 경우 ValuieError가 뜬다. 

**값을 제거한다.** 



#### .pop(i)

i에 있는 값을 삭제하고, 그 항목을 반환한다.

i가 지정되어 있지 않으면 마지막 항목을 삭제하고 반환하낟. 

**인덱스를 제거한다.** 



#### .clear()

모든 항목을 삭제한다. 



### 탐색

#### .index(x)

x 값을 찾아 인덱스 값을 반환한다. (위치를 알려준다.)



#### .count(x)

x 값의 개수를 반환한다. 



#### .sort()

원본 리스트를 정렬하고 None을 반환한다. 

sorted 함수와 비교해보자. 

```python
numbers = [3, ]
```



#### .reverse()

순서를 반대로 뒤집어준다. 

정렬을 하지 않는다.

None을 반환한다. 



#### 함수와 메서드 구별법

S.V() == 메서드 

* 점이 있다

sum() == 함수 



## 딕셔너리

* 키와 값이 쌍으로 이뤄져있다. 

### 조회

#### .get(key[.default])

키에 있는 값을 가져올 수 있다. 

디폴트 값(0)을 넣어주면 0을 출력한다.

```python
my_dict = {'apple': '사과', }
```



에러를 발생시키는 것이 있고 기본값을 설정하는 것이 있다. 

 

### 추가 및 삭제

#### .pop(key[.default])

* key가 딕셔너리에 있으면 제거하고 해당 값을 반환한다.
* 그렇지 않으면 디폴트를 반환한다. 



#### .update([other])



딕셔너리는 인덱스라는 개념이 없다. 

키라구요 키! 키로 접근을 하는 거라구요!

엥 진짜?

그럼 위에 인덱스로 접근한건 뭐여

왜죠?

**딕셔너리는 키로 접근하고 리스트는 인덱스로 접근한다.** 

딕셔너리끼리는 더하기를 할 수 없다. 

